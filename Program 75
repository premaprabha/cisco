#include <stdio.h>
#include <stdlib.h>

int main() {
    FILE *file;
    char line[200];
    int count = 0;

    
    file = fopen("records.txt", "r");
    if (file == NULL) {
        printf("Error opening file!\n");
        return 1;
    }


