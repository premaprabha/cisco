#include <stdio.h>

struct Course {
    char courseID[10];
    char courseName[50];
    char instructor[50];
    int credits;
};

int main() {
    int n, i;

    printf("Enter the number of courses to store: ");
    scanf("%d", &n);

    struct Course courses[n];  // Array to store multiple courses

    
    for(i = 0; i < n; i++) {
        printf("\n--- Enter details for Course %d ---\n", i + 1);
        printf("Course

