#include <stdio.h>
#include <stdlib.h>

int main() {
    FILE *file;
    char data[200];
    char filename[100];

    
    printf("Enter the file name to append data: ");
    scanf("%s", filename);


    file = fopen(filename, "a");
    if (file == NULL) {
        printf("Error: Cannot open file!\n");
        return 1;
    }


    printf("Enter data to append (press Enter to finish):\n");
    scanf("

return
